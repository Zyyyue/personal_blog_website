<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--这个写包名加上类名-->
<mapper namespace="com.xixizai.personalblogwebsite.mapper.MessageMapper">
    <!--        批量审核通过留言-->
    <update id="batchApproveMessages">
        update messages
        set is_approved=1, update_time=now()
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!--    批量删除留言-->
    <delete id="batchDeleteMessages">
        delete from messages
        where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!--    获取留言列表-->
    <select id="getMessagesList" resultType="com.xixizai.personalblogwebsite.pojo.vo.MessageVO">
        select * from messages
        where visitor_id=#{visitorId}
    </select>

    <!--    分页查询留言（平铺列表，用于组装树形结构）-->
    <select id="getAllMessages" resultType="com.xixizai.personalblogwebsite.pojo.vo.MessageVO">
        SELECT
            id,
            root_id as rootId,
            parent_id as parentId,
            parent_nickname as parentNickname,
            content_html as contentHtml,
            visitor_id as visitorId,
            nickname,
            email_or_qq as emailOrQq,
            location,
            user_agent_os as userAgentOs,
            user_agent_browser as userAgentBrowser,
            is_approved as isApproved,
            is_secret as isSecret,
            is_admin_reply as isAdminReply,
            create_time as createTime
        FROM messages
        WHERE is_approved = #{isApproved}
        ORDER BY create_time ASC
    </select>

</mapper>
