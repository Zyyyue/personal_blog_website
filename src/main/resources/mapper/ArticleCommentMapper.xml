<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--这个写包名加上类名-->
<mapper namespace="com.xixizai.personalblogwebsite.mapper.ArticleCommentMapper">

    <!--    批量审核通过评论-->
    <update id="batchApproveArticleComment">
        update article_comments
        set is_approved=1,update_time=now()
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>


    <!--    批量删除评论-->
    <delete id="batchDeleteArticleComment">
        delete  from article_comments
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>


    <!--    访客编辑评论-->
    <update id="updateContent">
        update article_comments set
        content_html=#{contentHtml},is_edited=1,
        content=#{content},update_time=now()
        where id=#{id}
    </update>

    <!--    查询根评论（平铺列表，用于组装树形结构）-->
    <select id="getRootComments" resultType="com.xixizai.personalblogwebsite.pojo.vo.ArticleCommentVO">
SELECT
    id,
    article_id as articleId,
    root_id as rootId,
    parent_id as parentId,
    parent_nickname as parentNickname,
    content_html as contentHtml,
    visitor_id as visitorId,
    nickname,
    email_or_qq as emailOrQq,
    location,
    user_agent_os as userAgentOs,
    user_agent_browser as userAgentBrowser,
    is_approved as isApproved,
    is_secret as isSecret,
    is_admin_reply as isAdminReply,
    create_time as createTime
FROM article_comments
WHERE is_approved = 1
  AND article_id = #{articleId}
ORDER BY create_time ASC
    </select>

    <!--    根据文章 ID 查询评论（平铺列表）-->
    <select id="getCommentsByArticleId" resultType="com.xixizai.personalblogwebsite.pojo.vo.ArticleCommentVO">
SELECT
    id,
    article_id as articleId,
    root_id as rootId,
    parent_id as parentId,
    parent_nickname as parentNickname,
    content_html as contentHtml,
    visitor_id as visitorId,
    nickname,
    email_or_qq as emailOrQq,
    location,
    user_agent_os as userAgentOs,
    user_agent_browser as userAgentBrowser,
    is_approved as isApproved,
    is_secret as isSecret,
    is_admin_reply as isAdminReply,
    create_time as createTime
FROM article_comments
WHERE article_id = #{articleId}
ORDER BY create_time ASC
    </select>

    <!--    查询文章的所有评论（平铺列表，用于组装树形结构）-->
    <select id="getAllCommentsByArticleId" resultType="com.xixizai.personalblogwebsite.pojo.vo.ArticleCommentVO">
SELECT
    id,
    article_id as articleId,
    root_id as rootId,
    parent_id as parentId,
    parent_nickname as parentNickname,
    content_html as contentHtml,
    visitor_id as visitorId,
    nickname,
    email_or_qq as emailOrQq,
    location,
    user_agent_os as userAgentOs,
    user_agent_browser as userAgentBrowser,
    is_approved as isApproved,
    is_secret as isSecret,
    is_admin_reply as isAdminReply,
    create_time as createTime
FROM article_comments
WHERE is_approved = #{isApproved}
<if test="articleId != null">
  AND article_id = #{articleId}
</if>
ORDER BY create_time ASC
    </select>

    <!--    管理端分页查询评论（平铺列表，不需要树形结构）-->
    <select id="pageQueryComments" resultType="com.xixizai.personalblogwebsite.pojo.vo.ArticleCommentVO">
SELECT
    id,
    article_id as articleId,
    root_id as rootId,
    parent_id as parentId,
    parent_nickname as parentNickname,
    content_html as contentHtml,
    visitor_id as visitorId,
    nickname,
    email_or_qq as emailOrQq,
    location,
    user_agent_os as userAgentOs,
    user_agent_browser as userAgentBrowser,
    is_approved as isApproved,
    is_secret as isSecret,
    is_admin_reply as isAdminReply,
    create_time as createTime
FROM article_comments
<where>
    <if test="articleId != null">
        AND article_id = #{articleId}
    </if>
    <if test="isApproved != null">
        AND is_approved = #{isApproved}
    </if>
</where>
ORDER BY create_time DESC
    </select>

</mapper>
